So what is hashing ? Hashing
In our day to day life searching is very common, any website we go the first thing we do is search for the product that we want to buy. Now in order to achieve this search a very simple way is to iterate over the array one by one and finally finding the element, so here for 1 query its is O(N), now one might think lets sort this array it would be easier to find then also for one query it is O(nlog2n). So this traditional way of searching an element is not that good so hashtable came into picture.
Hashtable makes the process of searching an element in an array fast

In JS we create a map like -> let myMap = new Map();
